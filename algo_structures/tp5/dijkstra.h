#include "types_graphe.h"

/* Calcule les plus courts chemins des sommets depuis le sommet de départ, en utilisant l'algorothme de Dijkstra. */
void parcours_dijkstra(graphe_matriciel g, int depart, int** distances, int** predecesseurs);

